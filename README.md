# NotePlanner

一个简洁的笔记管理面板首版，当前使用纯 HTML、CSS 和 JavaScript 实现，无需安装依赖。

## 本地预览

直接双击 `index.html` 即可打开；也可以在项目目录运行：

```powershell
python -m http.server 4173
```

然后访问 http://localhost:4173。

当前首版包含：笔记网格、关键词搜索、排序菜单、收藏交互、深色模式、响应式布局和新建笔记提示。
