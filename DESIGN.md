# NotePlanner 软件设计文档

---

## 1. 产品定位

本地优先的 Markdown 笔记管理工具，面向需要结构化知识体系构建的个人用户。

- **数据归属用户**：笔记以 `.md` 文件存于本地文件系统，不属于任何平台
- **核心体验**：三层组织（主题 → 笔记集 → 笔记），类似文件系统的目录树，但为笔记场景优化
- **风格**：克制、专注，避免装饰性元素干扰内容

---

## 2. 数据模型

### 命名

| 层级 | 原名 | 新名 | 说明 |
|---|---|---|---|
| L1 | 分组 | **主题** | 顶层知识领域分类（如：工作、生活、学习、游戏制作） |
| L2 | 聚合体 | **笔记集** | 某个主题下的子分类（如：渲染、美术、音乐） |
| L3 | 笔记 | **笔记** | 单篇 Markdown 文档 |

### 结构示例

```
游戏制作（主题）
  ├── 渲染（笔记集）
  │     ├── PBR 管线笔记
  │     ├── 后处理效果笔记
  │     └── 光照方案对比
  ├── 程序（笔记集）
  ├── 美术（笔记集）
  └── 音乐（笔记集）

工作（主题）
  └── 产品与工作（笔记集）
        ├── Q2 目标
        └── 设计系统学习笔记
```

### 各层能力

**主题**
- 新建、重命名、删除（弹窗确认，包含所有子内容）
- 置顶 / 取消置顶
- 折叠 / 展开（控制笔记集可见性）

**笔记集**
- 新建、重命名、删除（弹窗确认，笔记可选择进回收站或零散笔记）
- 置顶 / 取消置顶
- 折叠 / 展开（控制笔记列表可见性）
- 颜色标记、描述文本

**笔记**
- 新建、编辑（Markdown 实时预览）
- 删除 → 弹窗二选一：移入回收站（保留最新 N 篇）或 变为零散笔记
- 笔记集内拖拽排序（待实现，当前按文件读取顺序呈现）
- 无收藏功能（用排序代替收藏的场景）

### 独立结构

| 结构 | 说明 |
|---|---|
| **零散笔记** | 不归属任何笔记集的游荡笔记，需用户手动删除 |
| **回收站** | 自动保留最新 N 篇（默认 30，可在设置中修改），支持恢复或永久删除 |

---

## 3. 文件系统映射

### 目录结构

```
workspace/                          ← 默认工作区根目录
├── 工作/                           ← 主题目录
│   ├── .group-meta.json            ← 主题元数据（id, expanded, pinned）
│   └── 产品与工作/                 ← 笔记集子目录
│       ├── .collection-meta.json   ← 笔记集元数据（id, description, color, notesExpanded, updated）
│       ├── Q2目标.md               ← 笔记正文（纯 Markdown）
│       ├── Q2目标.md.meta.json     ← 笔记结构化元数据（id）侧车文件
│       └── 设计系统学习笔记.md
├── 零散笔记/                       ← 零散笔记专用目录
│   └── 某篇独立笔记.md             ← 同上（纯 Markdown + 同名 .meta.json 侧车）
└── 回收站/                         ← 回收站目录
    ├── 已删除笔记.md
    └── 已删除笔记.source.json      ← 来源信息（source, deletedAt）
```

### 规则

- 目录名 = 对象名（title），文件 / 目录名中的 `\ / : * ? " < > |` 等特殊字符用下划线替换。
- 笔记正文为纯 `.md` 文件；**笔记标题 = 本地文件名（不含 `.md` 扩展名）**，正文首行 `# 标题` 仅作 Markdown 排版惯例，不决定列表标题。
- **笔记结构化元数据存于同名侧车文件，不进笔记正文**：笔记的 `.md` 只写纯 Markdown 正文；其结构化字段（当前仅 `id`）以 JSON 写入与 `.md` 同名的 `.meta.json` 侧车文件（如 `Q2目标.md` ↔ `Q2目标.md.meta.json`）。这样笔记正文开头不会出现"一串未知信息"，且正文与元数据解耦。

  ````json
  // Q2目标.md.meta.json
  { "id": "note-abc123" }
  ````

  - 读取时 `parseMd()` 仍会剥离**遗留** front matter（`---{json}---`），保证旧文件正文不被污染；新写入的笔记不再生成 front matter。
  - **不再把元数据写进正文**，因此正文内的 `---` 分隔线不会被误判为 front matter（原隐患已消除）。
  - 同步（`sync()`）会在写 `.md` 的同时写/更新同名 `.meta.json`；删除笔记时会一并清理孤儿 `.meta.json`，避免重载后出现重复 / 残留。
- **主题 / 笔记集 / 回收站** 的元数据存于各自目录下的独立 `.json` 文件（见下表），与笔记正文解耦。

### 元数据字段

| 文件 | 字段 | 说明 |
|---|---|---|
| `.group-meta.json`（主题） | `id`, `expanded`, `pinned` | 主题唯一标识、是否展开、是否置顶 |
| `.collection-meta.json`（笔记集） | `id`, `description`, `color`, `notesExpanded`, `updated` | 标识、描述、颜色标记、笔记列表是否展开、更新时间 |
| `回收站/<标题>.source.json` | `source`, `deletedAt`, `id` | 来源位置（主题 › 笔记集）、删除时间、笔记唯一标识 |
| 笔记 `<标题>.md.meta.json` 侧车 | `id` | 笔记唯一标识，存于同名侧车文件，**不进 `.md` 正文**；由 `genId()` 生成（`Date.now` + 自增序号 + 随机串），保证会话内唯一，避免快速连续创建时 id 撞车导致笔记无法打开 |

### 扫描与丢弃规则

- `scan()` 仅保留**含笔记的笔记集**（`notes.length > 0`）与**含笔记集的主题**（`collections.length > 0`）；空笔记集 / 空主题在重新扫描时会被丢弃，不会长期保留在界面。
- `sync()` 在写盘后会**清理已不存在的主题 / 笔记集目录**（按 `.group-meta.json` / `.collection-meta.json` 中的 `id` 匹配）：被删除、重命名或"移入零散笔记"的笔记集不会因磁盘目录残留而在下次扫描时"复活"。
- **安全清理（2026-08-04，agent 集成前置）**：`sync()` 的孤儿清理改为"按侧车判断归属"——任何 `.md` / 主题目录 / 笔记集目录**只要没有对应的 `.meta.json` / `.group-meta.json` / `.collection-meta.json` 侧车，就视为外部 agent 新增内容、一律保留，绝不删除**；只有"有侧车且 `id` 已从内存模型移除 / 路径已变（重命名残留）"的文件或目录才会被真正删除。这样把 NotePlanner 工作目录同时当 agent 工作目录时，agent 新建的主题 / 笔记集 / 笔记不会被误删，而用户在 UI 内的删除、重命名仍正确落盘。

---

## 4. 功能模块

### 4.0 信息架构与导航

应用采用"侧边栏主导航 + 笔记集视图三栏"的结构：

- **侧边栏一级入口**：`笔记集` / `零散笔记` / `回收站` / `搜索` 四项并列。
- **主题树不单独成入口**：点开"笔记集"后，主题树位于其视图左侧面板内（见 §4.7）；主题 / 笔记集的增删改在树内完成。
- 侧边栏可整体收起（按钮收起 + 浮动按钮重新展开），与三栏折叠互不耦合。

### 4.1 搜索（独立视图）

搜索是侧边栏一级导航入口（第 4 项），点击切换到独立的搜索视图。

- **交互**：输入即搜索（建议 250ms 防抖），跨「主题 › 笔记集 › 笔记」全文匹配，匹配范围含笔记标题、正文与笔记集描述。
- **结果卡片**：每张卡片展示 `标题` + `路径（主题 › 笔记集）` + `内容片段（命中关键词高亮）`。
- **点击跳转**：自动切回"笔记集"视图 → 展开对应主题 → 展开并选中对应笔记集 → 打开并选中源笔记。
- **空态**：无输入时展示引导文案；无结果时展示"未找到相关笔记"。
- **现状**：独立搜索视图已完成（见 §7）。原笔记集视图内的 `#search-input` 过滤框已移除（避免与独立搜索视图重复）。

### 4.2 主题管理

- 操作入口在"笔记集"视图的主题树内（非独立主题视图）。
- 新建弹窗、重命名弹窗
- 删除弹窗警告"包含所有笔记集和笔记，不可撤销"
- 置顶 / 折叠
- 右键菜单

### 4.3 笔记集管理

- 操作入口在"笔记集"视图的笔记集面板内。
- 新建弹窗（可选择所属主题）
- 重命名弹窗
- 删除弹窗确认"笔记可选择进回收站或零散笔记"
- 颜色标记（coral / blue / green / purple）
- 描述文本
- 折叠 / 展开、置顶、拖拽移动（跨主题？待定）

### 4.4 笔记管理

- 新建弹窗（自动填入笔记集）
- Markdown 编辑 + 实时预览（编辑器内"预览"按钮切换，或左右分栏）
- 笔记集"更多操作"菜单含：**新增笔记** / **移入零散笔记**（将该集全部笔记转入零散笔记并移除该笔记集）/ 重命名 / 删除笔记集
- 删除弹窗二选一（移入回收站 / 变为零散笔记）
- **排序**：笔记在笔记集内的顺序当前按文件读取顺序呈现，**手动拖拽排序尚未实现**（待定，见 §7）。

### 4.5 零散笔记

- 列表展示，独立视图
- 可移动到任意笔记集（两个联动下拉框：先选目标主题，再在该主题下选笔记集）
- 手动删除

### 4.6 回收站

- 列表展示，含来源路径和删除时间（来自 `.source.json`）
- 可配置保留数量（设置面板）：保留最新 N 篇，超出自动清理
- 恢复：①「恢复到零散笔记」一步到位；②「恢复到指定笔记集」两个联动下拉框（先选目标主题，再在该主题下选笔记集）；永久删除、清空回收站
- **笔记集整集回收**：删除笔记集弹窗提供第三个选项「将整个笔记集放入回收站（保留结构）」——笔记集连同其全部笔记作为**一条 `type:'collection'` 回收项**进入回收站（标题前带「笔记集」徽标、左侧珊瑚色条区分）；恢复时菜单为「恢复为笔记集…」（选目标主题即可还原为含全部笔记的新笔记集，标题重名自动加 `(恢复)` 后缀）。单篇笔记回收项仍走①/②两条恢复路径。
- **删除真正落盘（2026-08-04 修复）**：`sync()` 回收站与零散笔记段落原来用 `if (trash && trash.length)` / `if (looseNotes && looseNotes.length)` 包裹，导致删空时整段跳过、磁盘残留文件在下次扫描时被"复活"，表现为"删除不生效"。现已改为始终执行写+清理：回收站按 `.source.json` 的 `id` 不在当前 `trash` 即删除对应 `.md`/`.source.json`；零散笔记在删空时也清理有 `.meta.json` 侧车的残留 `.md`。删除操作即时生效且重启后保持。
- **现状**：保留数量已持久化（2026-08-03）。设置面板中修改回收站保留数会即时调用 `persistSettings()`（经 `settings:save` IPC 写入 `settings.json`）并触发 `enforceTrashLimit()` 清理超额，重启后保持用户设定。

### 4.7 三栏布局（笔记集视图内）

笔记集视图主体由三栏组成，自左至右：

- **三栏各自独立滚动**：三栏容器高度锁定为视口高度（`.workspace`/`.workspace-loose` 设 `height:calc(100vh - 290px)` + grid 行高 `minmax(0,1fr)`），主题树栏 / 笔记集栏 / 编辑栏各自的 `overflow:auto` 生效。任一栏内容超出时仅在该栏内部滚轮滚动，不会把整页撑高、也不会因笔记集过多而看不到下方（修复"笔记集太多时下方笔记集不可见"问题，2026-08-04）。
- **主题树栏（左）**：展示主题与笔记集的层级树，可收起。
- **笔记集栏（中）**：选定某主题后，该栏展示**该主题下的所有笔记集**（每个笔记集一张卡片，当前选中项高亮）；笔记不单独成栏，而是嵌套在所属笔记集卡片之下。逻辑要点：
  - 选定主题 → 笔记集栏列出该主题**全部**笔记集（此前实现只渲染单个选中笔记集，已修正）。
  - 每张笔记集卡片内的**笔记列表至多展示 5 条**；超过 5 条时列表区域出现滚动条，滚轮可查看剩余笔记（`.notes-list` 设 `max-height` 约 5 条高度 + `overflow-y:auto`）。
  - 点击某笔记集卡片即选中该笔记集（其首条笔记载入右侧编辑器）；点击卡片内笔记项跳转编辑。
  - 点击卡片内某篇笔记时**仅切换高亮、不重建整个笔记集栏**，因此包含 >5 条笔记、处于滚动浏览状态的笔记列表不会被重置回置顶（修复"选中靠后笔记自动回滚至顶部"问题）。
  - 笔记集栏整体可随三栏折叠收起（见下）。
- **Markdown 栏（右）**：Markdown 编辑器 + 实时预览。

折叠与空间分配（收起后其余栏自动延展，聚焦编辑）：

- 收起**主题树栏** → 笔记集栏左移占据主题栏宽度，Markdown 栏相应变宽。
- 再收起**笔记集栏** → Markdown 栏进一步占据前两栏空间，达到最大编辑区。
- **折叠顺序约束**：必须先收起主题树栏，才能收起笔记集栏（即笔记集栏的收起依赖于主题树栏已收起）。
- 折叠开关位于**笔记集视图底部**的浮动圆形按钮组（`#collapse-dock` 中的 `#dock-collapse` 收起 ‹ / `#dock-expand` 展开 ›），仅图标无文字；收缩宽度经 `grid-template-columns .3s cubic-bezier(.22,.61,.36,1)` 平滑过渡。
- **渐进式显隐**：初始仅显示"收起"键（收起主题树）；收起一次后出现"收起"+"展开"两个键（可继续收起笔记集 / 展开主题树）；两栏均收起后仅剩"展开"键。由 `collapseStack` 按收起顺序管理（LIFO 展开），`canCollapsePanel` 强制顺序约束。
- 折叠为会话内临时聚焦操作，**不持久化**，每次启动回到默认展开态（见 §7）。
- 折扣说明：零散笔记 / 回收站视图**不再内置搜索框**（独立搜索视图已覆盖 §4.1）。

> 注：侧边栏（主导航：笔记集 / 零散笔记 / 回收站 / 搜索）可独立收起（按钮收起 + 浮动按钮重新展开），与三栏折叠互不耦合。

### 4.8 深色模式

- 全局主题切换（`#theme-toggle` 按钮切换 `body.dark`）。
- **现状**：主题选择已持久化（2026-08-03）。`#theme-toggle` 与设置面板主题分段控件均经 `persistSettings()` 落盘，启动时由 `loadSettings` 恢复并同步 `body.dark` 与按钮图标，重启后保持所选主题。

### 4.9 工作区迁移

- 切换工作区目录路径
- 可选择是否将现有数据迁移到新路径

---

## 5. 技术架构

```
┌─────────────────────────────────┐
│      Electron 桌面应用           │
│  主进程 + 渲染进程（三栏布局）   │
└──────────┬──────────────────────┘
           │ 读写
┌──────────▼──────────────────────┐
│    文件系统映射层                │
│  scan_workspace()  /  sync_workspace()        │
└──────────┬──────────────────────┘
           │
┌──────────▼──────────────────────┐
│    workspace/ 目录               │
│  主题/笔记集/.md 文件            │
└─────────────────────────────────┘
```

| **运行环境** | Node 22（托管版）+ Electron 43 |
| **启动** | `start.bat` → `electron .`（双击启动，已配置 npm 镜像） |
| **代码结构** | `main.js`(主进程：窗口/文件 IO/IPC) / `preload.js`(安全桥接，contextIsolation) / `index.html`+`app.js`+`styles.css`(渲染进程 UI 与逻辑) |

> 注：原设计文档为 PySide6（Python）方案，已于 2026-08-03 改为 Electron 实现并同步本表。

---

## 6. 数据流

```
应用启动
  → scan_workspace() 扫描文件系统
  → 渲染 UI（空工作区即空，不自动写入任何默认内容）
  → 同步监听文件系统变更（fs.watch），外部改动触发重新扫描

用户操作
  → 修改内存中的数据结构
  → 触发 schedule_sync()（3 秒防抖）
  → sync_workspace() 全量写回文件系统
  → 保存成功

工作区迁移
  → 修改 settings.workspacePath
  → 可选复制旧工作区数据（workspace:migrate 的 transfer 参数）
  → 重新 scan_workspace()
```

> 注：首次启动不再写入"默认演示笔记集"；若工作区为空，界面直接展示空状态，由用户自建内容。

---

## 7. 已知缺口（待设计 / 待实现）

| 项目 | 状态 |
|---|---|
| 侧边栏"标签"模块 | 已移除（DESIGN 未定义该功能，原为游离占位，2026-08-03 清理） |
| 独立搜索视图（§4.1） | 已完成（2026-08-04 实现并美化：侧边栏第 4 入口 `#nav-search`，全局搜索标题 + 正文，结果卡片含标题/路径/片段高亮（品牌珊瑚色 `mark`），左侧强调条 + hover 抬升；空状态图标化（`⌕` / `∅`）；点击跳转并展开对应主题/笔记集；Ctrl/Cmd+K 唤起，Enter 跳转首条） |
| 零散笔记视图 | 已完成（列表 / 编辑 / 搜索 / 移动到笔记集 / 彻底删除） |
| 回收站视图 | 已完成（2026-08-04 改造为与其他视图一致的大栏位面板 `.trash-panel`：panel-heading + 计数徽标 `#trash-count` + `.trash-list` 卡片列表；恢复 / 永久删除 / 清空，含来源与删除时间；视图内不再有搜索框） |
| 深色模式持久化（§4.8） | 已完成（2026-08-03：`#theme-toggle` 与设置面板主题选择经 `persistSettings()`→`settings:save` 落盘；启动经 `loadSettings` 恢复 `body.dark`；重启保持所选主题） |
| 设置面板持久化 | 已完成（2026-08-03：回收站保留数变更即时 `persistSettings()` + `enforceTrashLimit()` 落盘；工作区路径经 `workspace:migrate` 写入 `settings.json`；主题同 §4.8） |
| 三栏折叠交互（§4.7） | 已完成（2026-08-04：折叠开关改为笔记集视图底部浮动圆形按钮组 `#collapse-dock`（`#dock-collapse` 收起 / `#dock-expand` 展开），渐进式显隐——初始仅收起键，收起一次出现收起+展开，两栏收起后仅展开键；`collapseStack` LIFO + `canCollapsePanel` 顺序约束 + `dockCollapse/dockExpand` + `applyPanelState`/`renderCollapseDock` 级联变宽；宽度经 `grid-template-columns .3s cubic-bezier(.22,.61,.36,1)` 平滑过渡；不持久化，重启回默认展开态） |
| 笔记集内拖拽排序（§4.4） | 待定（当前按文件读取顺序，无手动排序） |
| 笔记集跨主题拖拽（§4.3） | 待定 |
| 笔记集栏展示主题下全部笔记集（§4.7） | 已完成（2026-08-05：修正原"只渲染单个选中笔记集"的逻辑错误，`renderCollections()` 改为遍历 `selectedGroup.collections` 渲染该主题下**所有**笔记集卡片；点击卡片即 `selectCollection` 选中，高亮当前项） |
| 笔记集卡片内笔记至多 5 条可滚动（§4.7） | 已完成（2026-08-05：`.notes-list` 设 `max-height`≈5 条高度 + `overflow-y:auto`，超过 5 条出现滚动条支持滚轮查看；点击委托改用 `data-collection` 定位所属笔记集） |
| 笔记元数据 front matter | 已消除（2026-08-05：笔记结构化元数据 `id` 移出 `.md` 正文、改存同名 `.meta.json` 侧车；`parseMd()` 仍剥离遗留 front matter 以兼容旧文件；新写入不再生成 front matter，正文内 `---` 不再被误判；`sync()` 写/更新 `.meta.json` 并清理孤儿侧车） |
| 删除笔记集后立即重渲染（§4.3/§4.4） | 已完成（2026-08-06：删除处理改用 `findGroupOfCollection(id)` 反查真实所属主题并同步 `selectedGroup`，删除后即时 `renderGroupTree/renderCollections/renderEditor/renderTrash/renderLooseNotes` 重渲染，修复"删除后需手动刷新才生效"） |
| 长笔记列表选中靠后笔记自动回顶（§4.7） | 已完成（2026-08-06：选中笔记改由 `selectNoteInCollection()` 仅切换 `.collection-note`/`.collection-card` 高亮，不再调用 `renderCollections()` 整栏重建，保留 `.notes-list` 滚动位置） |
| 移动到/恢复指定笔记集改为两个联动下拉框（§4.5/§4.6） | 已完成（2026-08-06：零散笔记「移动到笔记集」与回收站「恢复到指定笔记集」由平铺 `<select>` 改为 `openPickCollectionModal()` 双下拉框——主题下拉 + 笔记集下拉（第二个随第一个联动刷新）；后于 2026-08-06 由两步式改回双下拉框；新增 `moveLooseNoteToCollection`/`restoreTrashToCollection` 辅助函数） |
| 空工作区首次初始化 | 已移除"写入演示数据"设计（§6 改为空工作区即空） |
| 打包脚本 | 待实现（仅 start.bat 启动，无 build / 打包） |
| 收藏字段(favorite) | 已移除（与"无收藏功能"设计矛盾，2026-08-03 清理死代码） |
| agent 集成 · 外部改动自动刷新（§4.x 新增） | 已完成（2026-08-04：`preload.js` 暴露 `onWorkspaceChanged` 桥接 `workspace-changed`；`app.js` 监听后先 flush 待写编辑再 `scan()` 重扫，重解析 `selectedGroup/Collection/Note/looseNote` 引用并按 `currentView` 刷新对应视图（含零散/回收站），修复早期实现只替换数组不更新选中态、漏刷新非笔记集视图的问题；重启 `start.bat` 生效，缓存 `app.js?v=12`） |
| agent 集成 · sync 孤儿清理安全化（§3 扫描与丢弃规则） | 已完成（2026-08-04：`sync()` 改为"按侧车判断归属"——无 `.meta.json`/`.group-meta.json`/`.collection-meta.json` 侧车的文件/目录视为外部 agent 新增、一律保留；仅删"有侧车且 id 已从模型移除/路径已变（重命名残留）"者。隔离测试 9 项断言全部通过，确认 agent 新增的主题/笔记集/笔记不会被误删，而 UI 删除/重命名仍正确落盘） |
| 三栏各自独立滚动（§4.7） | 已完成（2026-08-04：`.workspace`/`.workspace-loose` 高度锁 `calc(100vh - 290px)` + grid 行高 `minmax(0,1fr)`，覆盖原 `min-height` 被内容撑开的缺陷；`.collections-panel`/`.group-tree-panel`/`.loose-list-panel` 既有 `overflow:auto` 生效，笔记集过多时整栏内部滚轮、不再把页面撑高；`.trash-panel` 加 `max-height`、`.editor-panel` 加 `overflow:hidden`，窄屏媒体查询重置 `height:auto;grid-template-rows:auto` 以免破坏堆叠布局；缓存 `styles.css?v=20`） |
| 删除真正落盘（零散/回收站删除不生效）（§4.5/§4.6） | 已完成（2026-08-04：`sync()` 回收站段与零散段原用 `if (trash && trash.length)` / `if (looseNotes && looseNotes.length)` 包裹，删空时整段跳过、磁盘残留文件在下次 `scan()` 时被"复活"，表现即"删除不生效"。改为始终执行写+清理：回收站按 `.source.json` 的 `id` 不在当前 `trash` 即删 `.md`+`.source.json`；零散删空也清理有 `.meta.json` 侧车的残留 `.md`。修复过程中同时统一了回收站侧车命名约定为 `safeName(title)+'.source.json'`（与 `loadTrashNote` 的 `file.replace('.md','.source.json')` 读取方式一致，避免清理/读取找不到侧车）。隔离测试 13 项断言全部通过。缓存 `main.js` 改动需重启主进程、`app.js?v=12→v=13`、`styles.css?v=21→v=22`） |
| 笔记集整集放入回收站（§4.6） | 已完成（2026-08-04：删除笔记集弹窗新增第三个选项「将整个笔记集放入回收站（保留结构）」，整集作为 `type:'collection'` 回收项（含 notes 结构）进入回收站，回收站 UI 以「笔记集」徽标 + 左侧珊瑚色条区分；恢复菜单为「恢复为笔记集…」（选主题还原为含全部笔记的新笔记集，重名自动加 `(恢复)`）；新增 `openRestoreCollectionToGroup`/`restoreTrashCollection`，`loadTrashNote` 透传 `type`/`collection`） |
| Markdown 预览引擎升级（§3 渲染） | 已完成（2026-08-06：原手写逐行解析器（仅 `#`/`##`/`###`、`-` 列表、`>` 引用、`**粗体**`）替换为 markdown-it + DOMPurify（vendor/ 本地离线库），支持 GFM 表格、代码块、链接、图片、有序列表；渲染前经 DOMPurify 消毒。`.markdown-preview` 增补表格/代码块/链接/列表/分隔线/标题层级珊瑚色样式（含深色）；缓存 `styles.css?v=22→v=24`、`app.js?v=13→v=14` |
| 删除/恢复立即落盘（§4.3/§4.5/§4.6） | 已完成（2026-08-06：永久删除、删除笔记、删除笔记集、零散彻底删除、移入回收站、恢复到零散/笔记集、整集恢复等所有删除/恢复操作由 3 秒防抖 `scheduleSyncToWorkspace()` 改为 `clearTimeout(saveSyncTimer); syncToWorkspace()` 立即落盘，消除"删除后 rescan 从旧磁盘读回导致删除不生效/还在原位置"的竞态；`permanentDelete` 在确认时按 `id` 重新 `findIndex`，避免弹窗打开到确认间发生重扫导致删错/删不掉） |
| 笔记列表滚动跳顶修复（§4.7） | 已完成（2026-08-06：`renderCollections()` 重建前保存各 `.notes-list[data-collection]` 与容器 `scrollTop`，重建后恢复，修复 `workspace-changed` 重扫触发整栏重建导致长笔记列表滚动位置被重置回置顶） |
| Markdown 编辑区半宽修复（§3 渲染） | 已完成（2026-08-06：`.editor-body` 由 `grid-template-columns:1fr 1fr` 改为 `display:flex`，编辑/预览单显时撑满整栏，不再只占 50%） |
